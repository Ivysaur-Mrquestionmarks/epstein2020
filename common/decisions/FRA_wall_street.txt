FRA_the_wall_street_block_decision = {

	FRA_break_the_block = {
	
		icon = generic_civil_support

		available = {
			has_country_flag = FRA_wall_street_block
			hidden_trigger = {
				NOT = { 
					OR = {
						has_country_flag = negotiating_with_wall_street_block
						has_country_flag = dostum_is_invading_nyc
					}
				}
			}
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 100
		}

		cost = 50

		complete_effect = {
			add_manpower = -2000
			add_stability = -0.15
			annex_country = {
			    target = NYC
			    transfer_troops = no
			}
			remove_ideas = FRA_wall_street_block
			clr_country_flag = FRA_wall_street_block
		}

	}

	FRA_negotiate_with_the_block = {

		icon = generic_civil_support

		available = {
			has_country_flag = FRA_wall_street_block
			hidden_trigger = {
				NOT = { 
					has_country_flag = has_country_flag = dostum_is_invading_nyc
				}
			}
		}

		fire_only_once = yes

		cost = 125

		ai_will_do = {
			factor = 0
		}

		days_remove = 30

		complete_effect = {
			add_stability = -0.05
			hidden_effect = {
				set_country_flag = negotiating_with_wall_street_block
			}
		}

		remove_effect = {
			add_stability = 0.05
			annex_country = {
			    target = NYC
			    transfer_troops = no
			}
			remove_ideas = FRA_wall_street_block
			clr_country_flag = FRA_wall_street_block
		}


	}

	FRA_dostum_plan = {

		icon = generic_civil_support

		available = {
			has_country_flag = FRA_wall_street_block
			hidden_trigger = {
				NOT = { 
					has_country_flag = negotiating_with_wall_street_block
				}
			}
		}
		visible = {
			has_country_flag = fra_dostum_is_general
		}

		fire_only_once = yes

		ai_will_do = {
			factor = 0
		}

		cost = 10

		days_remove = 30

		complete_effect = {
			add_war_support = 0.05
			set_country_flag = dostum_is_invading_nyc
		}

		remove_effect = {
			add_war_support = -0.05
			            XXX = {
             	   if = {
	                    limit = {
	                        OR = {
	                            has_dlc = "Together for Victory"
	                            has_dlc = "Man the Guns"
	                        }
	                    }
	                    set_autonomy = {
	                        target = NYC
	                        autonomous_state = autonomy_puppet
	                        freedom_level = 0.25
	                    }
	                    else = {
	                        puppet = NYC
	                    }
	                }
	            }
			NYC = {
				hidden_effect = {
					every_unit_leader = { demote_leader = yes }
				}
				create_country_leader = {
					name = "Abdul Dostum"
					picture = "gfx/leaders/FRA/dostum.dds"
					expire = "2060.1.1"
					ideology = liberal
					traits = {
						###
					}
				}
				set_cosmetic_tag = NYC_new_kabul
			}
			every_unit_leader = {
				limit = {
					has_id = 36
				}
				set_nationality = NYC
				promote_leader = yes
			}
			remove_ideas = FRA_wall_street_block
			clr_country_flag = FRA_wall_street_block
		}


	}


}