### This is meant for countries that have collapses and if u want to have these wargoals streamlined instead of cluttering code :)
# From Turklander

## Texas and Texas Collapse
War_on_texas_collapse_tags = { #using global.texas_collapse
	for_each_scope_loop = { #This loops declare the wars
    	array = global.texas_collapse
		ROOT = {
			if = { 
				limit = {
					NOT = { 
						has_war_with = PREV 
					}
				}
				create_wargoal = {
    					target = PREV
    					type = annex_everything
				}
			}
		}
	}
	custom_effect_tooltip = texas_war_tt
}

### CSA Collapse Wargoals
RVR_kill_the_CSA = {
	custom_effect_tooltip = Wargoal_on_All_Confederates
	for_each_scope_loop = { #This loops declare the wars
    		array = global.south_collapse
		ROOT = {
			if = { 
				limit = {
					NOT = { 
						has_war_with = PREV 
					}
				}
				create_wargoal = {
    					target = PREV
    					type = annex_everything
				}
			}
		}
	}
}
